
# Features

- Add a packaging/install builder

# Bugs

- When coming back to the folder view from either display views, the folder view that was focused before switching to display view must be focused again.
- When in sync mode with folders that have a lot of differences, the selected item displays (sometimes) two overlayed names.
- Remove flickering when browsing images.
